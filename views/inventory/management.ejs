<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%- messages() %>

<a href="./newclassification">Add New Classification</a> 

<a href="./addinventory">Add New Vehicle</a>

