<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<!-- make message work -->
<%# message %>
<div class="form">
  <form method="post" action="#">
    <fieldset>
      <label>Email Address: <input type="email" name="email" placeholder="yourEmail@place.com" pattern="[a-z0-9._%+\-]+@[a-z0-9.\-]+\.[a-z]{2,}$" required></label>
      <label>Password: <input type="password" name="password" pattern="[a-zA-Z0-9]{12,}" required> </label>
      <!-- l<p>Password must be minimum of 12 characters and include 1 capital letter, 1 number and 1 special character.</p>       -->
      <input type="submit" value="Sign In!">
    </fieldset>
    
    <p>No account? <a href="#" >Sign-up</a></p>
  </form>

</div>

